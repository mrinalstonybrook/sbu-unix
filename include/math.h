#ifndef _MATH_H
#define _MATH_H

#include <defs.h>
int matchString( char *s , char *t);
int  atoi( char *s);
int strlen(const char *s);

int strcmp(char *s1, char *s2);
int pow(int x, int y);
int getSize(char *p) ;
int top(float x);
#endif
